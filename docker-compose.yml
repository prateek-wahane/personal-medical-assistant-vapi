version: "3.9"

services:
  medical-assistant:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./credentials:/app/credentials
      - ./medical_assistant.db:/app/medical_assistant.db
